var storago = {};
;(function(storago){

   var tables = {};

   var Entry = function(name, prop){

   };

   Entry.prototype.hasMany = function(dep_name, dep_table, parent_table){

   };

   var define = function(name, prop){

      var table = Entry();
   };
   storago.define = define;




   var schema = function(callback){

   };
   storago.syncSchema = schema;

}(storago));
